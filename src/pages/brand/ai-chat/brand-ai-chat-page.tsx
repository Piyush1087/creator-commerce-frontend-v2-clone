import { AiChatWorkspace } from "../../../features/ai-chat/components/AiChatWorkspace";

/** Prototype intake page — Gemini/Stitch UI, not Aurora yet. */
export function BrandAiChatPage() {
  return <AiChatWorkspace />;
}
