# Brand onboarding entry pages

Thin route entrypoints live next to this README as `brand-onboarding-*-page.tsx`
files. Heavy UI is implemented under `src/features/brand-onboarding`.

The default app entry is `/` for this slice until a new global entry point is
introduced.
